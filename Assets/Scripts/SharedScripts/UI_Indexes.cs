enum TitleMenuIndex {
    MAIN_TITLE = 0,
    SETTINGS = 1,
    GAME_MODES = 2
};

enum GeneralMenuIndex {
    PAUSE = 0
}

enum PlaneMenuIndex {
    GAME_OVER = 0
}

enum HeliMenuIndex {
    WIN = 0, 
    LOSE = 1
}